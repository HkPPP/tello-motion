# tello-motion

Phase-based Tello drone gesture-control project.

## Phase 1 status

- Modular runtime scaffold created under `src/`.
- Thin bootstrap entrypoint in `main.py`.
- Dry-run mode available to validate startup without a drone.

## Run

```bash
python main.py --dry-run
```
